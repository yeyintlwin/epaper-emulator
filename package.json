{
  "name": "restaurant-order-system",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "test": "npm --prefix apps/epaper-hub test",
    "demo:epaper": "npm --prefix apps/epaper-hub run demo"
  }
}
